import {adder} from '../adder'

describe('Adder', () => { // eslint-disable-line no-undef
  test('adds two numbers', () => {// eslint-disable-line no-undef
    expect(adder(5, 3)).toEqual(8)// eslint-disable-line no-undef
  })
})